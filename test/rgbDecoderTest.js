
(function(colorImageDecoder) {
    module("rgbDecoderTest.test");

    test("test", function() {
        // Arrange
        var foo = 1;

        // Act

        // Assert
        ok(foo, "construction did not return object");
    });

})(colorImageDecoder);